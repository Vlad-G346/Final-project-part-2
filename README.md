# Практический блок: вторая часть

Доступ к базе данных осуществляется с помощью команды psql -U morty -d scooter_rent.
Работа с базой данных проводилась через Терминал macOS.
Автотесты спроектированы на языке Python 3.9.6.
Для запуска тестов должны быть установлены библиотеки тестирования pytest и библиотеки HTTP-запросов requests.

# Задание 1

Представь: тебе нужно проверить, отображается ли созданный заказ в базе данных.
Для этого: выведи список логинов курьеров с количеством их заказов в статусе «В доставке» (поле inDelivery = true). 

Решение:
scooter_rent=# SELECT c.login, COUNT(o.id) AS orders_in_delivery FROM "Couriers" c JOIN "Orders" o ON c.id = o."courierId" WHERE o."inDelivery" = true GROUP BY c.login;
Результат выполнения в терминале можно посмотреть на скриншоте "Задание 1 Отображение заказа"

# Задание 2

Ты тестируешь статусы заказов. Нужно убедиться, что в базе данных они записываются корректно.
Для этого: выведи все трекеры заказов и их статусы. 
Статусы определяются по следующему правилу:
Если поле finished == true, то вывести статус 2.
Если поле canсelled == true, то вывести статус -1.
Если поле inDelivery == true, то вывести статус 1.
Для остальных случаев вывести 0.

Решение:
scooter_rent=# SELECT track, CASE WHEN "finished" = true THEN 2 WHEN "cancelled" = true THEN -1 WHEN "inDelivery" = true THEN 1 ELSE 0 END AS status FROM "Orders";
Результат выполнения в терминале можно посмотреть на скриншоте "Задание 2 Статусы заказов"

# Автоматизация теста к API

Теперь автоматизируй сценарий, который подготовили коллеги-тестировщики:
Клиент создает заказ.
Проверяется, что по треку заказа можно получить данные о заказе.
Шаги автотеста:
1: Выполнить запрос на создание заказа.
2: Сохранить номер трека заказа.
3: Выполнить запрос на получение заказа по треку заказа.
4: Проверить, что код ответа равен 200.

Решение представлено в данном репозитории. С результатом выполнения теста можно ознакомиться в скриншоте "Результат запуска теста"